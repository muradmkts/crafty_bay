import 'package:flutter/material.dart';

class ProductListByCategories extends StatelessWidget {
  const ProductListByCategories({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center()
    );
  }
}
